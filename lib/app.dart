import 'package:flutter/material.dart';

//  Entry point for the MES app
class MauritiusEmergencyServices extends StatelessWidget {
  const MauritiusEmergencyServices({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      "Welcome to Mauritius Emergency Services",
      textDirection: TextDirection.ltr,
    );
  }
}
